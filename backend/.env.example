# Server Configuration
# Port the server listens on (default: 3001)
PORT=3001

# Environment mode: development, testing, or production (default: development)
NODE_ENV=development

# Nano Banana API
# API key for Nano Banana image generation service
# Get this from https://www.nanobananai.com/
NANO_BANANA_API_KEY=your_api_key_here

# Frontend URL (for CORS)
# URL where the frontend is hosted (used to validate cross-origin requests)
# Examples:
#   Development: http://localhost:5173
#   Production: https://your-app.vercel.app
FRONTEND_URL=http://localhost:5173

# Supabase Configuration (PostgreSQL Database + File Storage)
# All required for production deployment

# Supabase project URL
# Format: https://[project-id].supabase.co
# Get this from your Supabase project settings
SUPABASE_URL=https://your-project.supabase.co

# Supabase anonymous key for client-side access
# Get this from your Supabase project settings (anon/public key)
SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase storage bucket name for image uploads
# This bucket must exist in your Supabase project
# Example bucket names: images, uploads, assets
SUPABASE_BUCKET_NAME=images

# Logging Configuration
# Log level: debug (most verbose), info, warn, error (least verbose)
# - debug: Detailed diagnostic information (development only)
# - info: Informational messages (default, recommended for production)
# - warn: Warning messages for unexpected conditions
# - error: Error messages only
LOG_LEVEL=info
